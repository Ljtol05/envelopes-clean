/* eslint-env node */
/* global console */
// Legacy local proxy removed. This stub file remains only to avoid stale references
// in some development environments or cached import graphs. It can be deleted.
// Keeping a console warning so devs notice if something still imports it.
console.warn('[placesProxy] This legacy local proxy has been deprecated. Use backend Places service instead.');

# Copy this file to .env and fill in values. Vite loads VITE_* at build+runtime.
###############################################
# Core API Configuration
###############################################
# Backend base URL (e.g. https://envelopes-backend.ashb786.repl.co/api or http://localhost:5000/api)
VITE_API_BASE_URL=

# Optional: Explicit SSE events URL (defaults to $VITE_API_BASE_URL/events if blank)
VITE_EVENTS_URL=

# Optional: Replit dev headers used by the API client in dev for identification
VITE_REPLIT_USER_ID=
VITE_REPLIT_USER_NAME=

# Optional: set to true to bypass auth during local dev (routes go straight to app)
VITE_DEV_BYPASS_AUTH=false

# Optional environment indicator (mirrors import.meta.env.MODE but can be used in logs)
VITE_NODE_ENV=development

###############################################
# Notes
# 1. Only VITE_* variables are exposed to the browser.
# 2. Leave optional values blank unless you specifically need them.
# 3. After changing this file restart `npm run dev` to ensure Vite picks up changes.
###############################################

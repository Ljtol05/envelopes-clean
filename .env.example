# Copy this file to .env and fill in values. Vite loads VITE_* at build+runtime.
###############################################
# Core API Configuration
###############################################
# Preferred new backend base URL (e.g. https://envelopes-backend.ashb786.repl.co/api or http://localhost:5000/api)
VITE_API_URL=

# (Legacy alias – only used if VITE_API_URL is blank; safe to leave empty)
VITE_API_BASE_URL=

# Optional: Explicit SSE events URL (defaults to $VITE_API_BASE_URL/events if blank)
VITE_EVENTS_URL=

# Optional: Replit dev headers used by the API client in dev for identification
VITE_REPLIT_USER_ID=
VITE_REPLIT_USER_NAME=

# Optional: set to true to bypass auth during local dev (routes go straight to app)
VITE_DEV_BYPASS_AUTH=false

# Optional: show floating Endpoint Diagnostics panel (lists resolved endpoint paths & env sources)
VITE_SHOW_ENDPOINT_DIAGNOSTICS=false

# Optional environment indicator (mirrors import.meta.env.MODE but can be used in logs)
VITE_NODE_ENV=development

###############################################
# Notes
# 1. Only VITE_* variables are exposed to the browser.
# 2. Leave optional values blank unless you specifically need them.
# 3. After changing this file restart `npm run dev` to ensure Vite picks up changes.
# 4. Advanced: You can override individual auth/KYC endpoint paths if backend uses non-standard routes.
#    Uncomment & customize any of the lines below (they default to the shown fallbacks):
# VITE_REGISTER_ENDPOINT=/api/auth/register
# VITE_LOGIN_ENDPOINT=/api/auth/login
# VITE_VERIFY_EMAIL_ENDPOINT=/api/auth/verify-email
# VITE_RESEND_EMAIL_ENDPOINT=/api/auth/resend-verification
# VITE_START_PHONE_VERIFICATION_ENDPOINT=/api/auth/start-phone-verification
# VITE_VERIFY_PHONE_ENDPOINT=/api/auth/verify-phone
# VITE_RESEND_PHONE_ENDPOINT=/api/auth/resend-phone-code
# VITE_ME_ENDPOINT=/api/auth/me
# VITE_START_KYC_ENDPOINT=/api/kyc/start
# VITE_KYC_STATUS_ENDPOINT=/api/kyc/status
###############################################

###############################################
# Google Places Autocomplete (Optional)
# Provide a restricted key to enable address suggestions in the KYC form.
# Leave blank to disable (form degrades gracefully without suggestions).
VITE_GOOGLE_PLACES_API_KEY=
###############################################
